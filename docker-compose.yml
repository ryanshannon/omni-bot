services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./prompts:/app/prompts
    environment:
      - APP_NAME=OmniAgent
      - APP_VERSION=0.1.0
    env_file:
      - .env
    restart: unless-stopped
